<?php
/**
* Implements hook_theme().
*/
function forcontu_theming_theme($existing, $type, $theme, $path) {
	$theme['hello_page'] = [
    'variables' => ['name' => NULL],
    'template' => 'hello',
  ];

  return $theme;
}